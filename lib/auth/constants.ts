/** Short-lived cookie set when establish-session succeeds after login. */
export const LOGIN_SUCCESS_COOKIE = 'frontbill_login_success'

/** Legacy sessionStorage key (login page may still set; toast reads cookie first). */
export const LOGIN_SUCCESS_TOAST_KEY = 'frontbill_login_success_toast'
