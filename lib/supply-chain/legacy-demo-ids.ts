/** IDs from the old browser-local supply chain demo seed (removed). Used only to purge stale localStorage. */
export const LEGACY_DEMO_RECIPE_IDS = new Set([
  'rcp-pepper-chicken',
  'rcp-jollof',
  'rcp-egusi',
])

export const LEGACY_DEMO_BATCH_IDS = new Set(['bat-jollof-1', 'bat-breakfast-1'])

export const LEGACY_DEMO_KITCHEN_STOCK_IDS = new Set(['ks-pepper', 'ks-jollof', 'ks-egusi'])
